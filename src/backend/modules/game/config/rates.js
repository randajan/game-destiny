
export const rates = {
    refresh:.3,         //ticker.interval = 1000 * rate.frefresh
    refreshShelly:10,    // refresh shelly lights every n ticks
    speed:.01,            //speed of the game = main rate .3
    entropy:1.6,          //all stats entropy
    decay:1.5             //all nodes decay
}

